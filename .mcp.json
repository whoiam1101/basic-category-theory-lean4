{
  "mcpServers": {
    "axle": {
      "type": "stdio",
      "command": "uvx",
      "args": ["--from", "axiom-axle-mcp", "axle-mcp-server"],
      "env": {
        "AXLE_API_KEY": "${AXLE_API_KEY}"
      }
    }
  }
}
